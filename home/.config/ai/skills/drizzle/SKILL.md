---
name: drizzle
description: Drizzle ORM for backend database queries and schema.
---

- Use drizzle-orm `db.select()` instead of findMany()
- Prefer leftJoin, innerJoin over many query
- Utilize `pickColumns` in db.select() or returning()
